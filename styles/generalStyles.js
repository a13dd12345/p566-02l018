import { StyleSheet } from 'react-native';

import { Colors } from '../utils';

export default StyleSheet.create({
  textH1: {
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
  },
  text: {
    fontSize: 14,
    textAlign: 'center',
  },
  textBold: {
    fontWeight: 'bold',
  },
});
